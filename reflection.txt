It is better for the calculate_tax function to return a value instead of printing it directly because the returned value can be reused in different parts of the program. For example, if the manager wants the tax to be saved into a file instead of displayed on the screen, the returned value can easily be used for that purpose without changing the calculate_tax function. This makes the function more reusable and easier to maintain.